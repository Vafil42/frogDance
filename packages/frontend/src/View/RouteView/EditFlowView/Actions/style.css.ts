import { style } from "@vanilla-extract/css";

export const wrapperStyles = style({
    paddingTop: 20,
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    width: 300,
})