import { style } from "@vanilla-extract/css";

export const wrapperStyles = style({
    display: "flex",
    flexDirection: "column",
    width: 500,
})

export const flowStyles = style({
    display: "flex",
})

export const fieldStyles = style({
    marginTop: 10,
})
